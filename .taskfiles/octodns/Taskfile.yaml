# yaml-language-server: $schema=https://taskfile.dev/schema.json
---
version: "3"

tasks:
  validate-internal:
    desc: Validate internal records
    cmd: uv run octodns-validate --config-file=./internal/config.yaml --all
  sync-internal:
    desc: Sync internal records
    cmd: uv run octodns-sync --config-file=./internal/config.yaml --doit --force
  validate-external:
    desc: Validate external records
    cmd: uv run octodns-validate --config-file=./external/config.yaml --all
  sync-external:
    desc: Sync external records
    cmd: uv run octodns-sync --config-file=./external/config.yaml --doit --force
