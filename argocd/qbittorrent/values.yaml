extraEnv:
  - name: QBT_EULA
    value: accept
volumes:
  - name: data
    persistentVolumeClaim:
      claimName: media-storage
volumeMounts:
  - name: data
    mountPath: /downloads
    subPath: downloads
