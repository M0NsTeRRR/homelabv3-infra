---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: looking-glass
spec:
  chartRef:
    kind: OCIRepository
    name: looking-glass
  interval: 1h
  values:
    podAnnotations:
      reloader.stakater.com/auto: "true"

    extraEnvFrom:
      - configMapRef:
          name: looking-glass
      - secretRef:
          name: &secret-ref hyperglass-openbao

    volumes:
      - name: &volume-config looking-glass-files
        configMap:
          name: looking-glass-files

    volumeMounts:
      - name: *volume-config
        mountPath: /etc/looking-glass/unicornafk.svg
        subPath: unicornafk.svg
        readOnly: true

    route:
      main:
        enabled: true
        kind: HTTPRoute
        apiVersion: gateway.networking.k8s.io/v1
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/group: ASN
          gethomepage.dev/name: looking-glass
          gethomepage.dev/description: Looking glass
        parentRefs:
          - name: external
            namespace: gateway
        hostnames:
          - lg.as212510.net

    valkey:
      auth:
        enabled: true
        usersExistingSecret: *secret-ref
        aclUsers:
          default:
            passwordKey: "LOOKING_GLASS_REDIS_PASSWORD"
      metrics:
        enabled: true
        serviceMonitor:
          enabled: true
        prometheusRule:
          enabled: true
          rules:
            - alert: ValkeyDown
              annotations:
                summary: Valkey instance {{ "{{ $labels.instance }}" }} down
                description: Valkey instance {{ "{{ $labels.instance }}" }} is down.
              expr: |
                redis_up{service="{{ include "valkey.fullname" . }}-metrics"} == 0
              for: 2m
              labels:
                severity: error
            - alert: ValkeyMemoryHigh
              annotations:
                summary: Valkey instance {{ "{{ $labels.instance }}" }} is using too much memory
                description: |
                  Valkey instance {{ "{{ $labels.instance }}" }} is using {{ "{{ $value }}" }}% of its available memory.
              expr: |
                redis_memory_used_bytes{service="{{ include "valkey.fullname" . }}-metrics"} * 100
                /
                redis_memory_max_bytes{service="{{ include "valkey.fullname" . }}-metrics"}
                > 90 <= 100
              for: 2m
              labels:
                severity: error
            - alert: ValkeyKeyEviction
              annotations:
                summary: Valkey instance {{ "{{ $labels.instance }}" }} has evicted keys
                description: |
                  Valkey instance {{ "{{ $labels.instance }}" }} has evicted {{ "{{ $value }}" }} keys in the last 5 minutes.
              expr: |
                increase(redis_evicted_keys_total{service="{{ include "valkey.fullname" . }}-metrics"}[5m]) > 0
              for: 1s
              labels:
                severity: error
