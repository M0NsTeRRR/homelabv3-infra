---
extraEnv:
  - name: TZ
    value: Europe/Paris
volumes:
  - name: &volume-data data
    persistentVolumeClaim:
      claimName: media-storage
volumeMounts:
  - name: *volume-data
    mountPath: /tv
    subPath: tv
  - name: *volume-data
    mountPath: /downloads
    subPath: downloads