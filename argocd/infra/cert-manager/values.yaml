crds:
  enabled: true
extraArgs:
  - enable-gateway-api
serviceIPFamilyPolicy: "PreferDualStack"
webhook:
  networkPolicy:
    enabled: false
  serviceIPFamilyPolicy: "PreferDualStack"
deploymentAnnotations:
  reloader.stakater.com/auto: "true"
prometheus:
  enabled: true
  servicemonitor:
    enabled: true
