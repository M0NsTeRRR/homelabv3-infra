---
apiVersion: v1
kind: Secret
metadata:
  name: seerr-postgres-cert
  labels:
    cnpg.io/reload: ""
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: seerr-postgres-cert
spec:
  secretName: seerr-postgres-cert
  privateKey:
    algorithm: RSA
    size: 4096
  usages:
    - server auth
  dnsNames:
    - seerr-cluster-rw
    - seerr-cluster-rw.default
    - seerr-cluster-rw.default.svc
    - seerr-cluster-r
    - seerr-cluster-r.default
    - seerr-cluster-r.default.svc
    - seerr-cluster-ro
    - seerr-cluster-ro.default
    - seerr-cluster-ro.default.svc
  issuerRef:
    name: openbao
    kind: ClusterIssuer
---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: seerr-cluster
spec:
  imageName: ghcr.io/cloudnative-pg/postgresql:17.6@sha256:c661099d1296a1e2348e3f9a730688f019260d01781a0196e4ed3fb578525215

  primaryUpdateStrategy: unsupervised
  instances: 1
  enablePDB: false

  enableSuperuserAccess: false

  certificates:
    serverTLSSecret: seerr-postgres-cert
    serverCASecret: seerr-postgres-cert

  storage:
    size: 2Gi
    pvcTemplate:
      dataSourceRef:
        apiGroup: volsync.backube
        kind: ReplicationDestination
        name: db-restore

  bootstrap:
    initdb:
      database: seerr
      owner: seerr
      secret:
        name: seerr-postgres-openbao
      dataChecksums: true
      encoding: UTF8

  monitoring:
    enablePodMonitor: true
