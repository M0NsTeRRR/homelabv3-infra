---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-storage
  namespace: radarr
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi # dummy value
  storageClassName: media-smb
