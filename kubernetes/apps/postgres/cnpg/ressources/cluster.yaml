---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgres
spec:
  imageName: ghcr.io/cloudnative-pg/postgresql:18.6-system-trixie@sha256:b1fd8d835001de14305c3ee0b25a75f25272a5829030a55bebb3bada711aaa75

  certificates:
    serverCASecret: postgres-server-cert
    serverTLSSecret: postgres-server-cert
    clientCASecret: postgres-client-cert
    replicationTLSSecret: postgres-client-cert

  storage:
    storageClass: ceph-block
    size: 20Gi

  # bootstrap:
  #   recovery:
  #     source: &source source

  plugins:
    - name: &plugin barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: &store rustfs-store

  # externalClusters:
  #   - name: *source
  #     plugin:
  #       name: *plugin
  #       parameters:
  #         barmanObjectName: *store
  #         serverName: postgres

  postgresql:
    pg_hba:
      - hostssl all all all cert clientcert=verify-full map=pooler-map

    pg_ident:
      - pooler-map pooler-rw all

  managed:
    roles:
      - name: authentik
        ensure: present
        login: true
        superuser: false
        disablePassword: true
      - name: bazarr
        ensure: present
        login: true
        superuser: false
        disablePassword: true
      - name: prowlarr
        ensure: present
        login: true
        superuser: false
        disablePassword: true
      - name: radarr
        ensure: present
        login: true
        superuser: false
        disablePassword: true
      - name: seerr
        ensure: present
        login: true
        superuser: false
        disablePassword: true
      - name: sonarr
        ensure: present
        login: true
        superuser: false
        disablePassword: true
