---
extraEnv:
  - name: TZ
    value: Europe/Paris
volumes:
  - name: &volume-data data
    persistentVolumeClaim:
      claimName: media-storage
volumeMounts:
  - name: *volume-data
    mountPath: /movies
    subPath: movies
  - name: *volume-data
    mountPath: /downloads
    subPath: downloads