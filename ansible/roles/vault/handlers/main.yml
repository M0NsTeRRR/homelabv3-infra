---
- name: Daemon-reload
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart vault.service
  ansible.builtin.systemd:
    name: vault.service
    state: restarted

- name: Enable vault.service
  ansible.builtin.systemd:
    name: vault.service
    enabled: true
