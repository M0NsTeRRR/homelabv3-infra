image:
  tag: "{{ traefik_version }}"
deployment:
  kind: Deployment
  replicas: 3
service:
  annotations:
    metallb.universe.tf/loadBalancerIPs: {{ traefik_external_ips | join(",") }}
  ipFamilyPolicy: PreferDualStack
providers:
  kubernetesIngress:
    publishedService:
      enabled: true
ingressClass:
  enabled: {{ traefik_default_ingress_class }}
additionalArguments:
  - "--entrypoints.web.http.redirections.entryPoint.to=websecure"
